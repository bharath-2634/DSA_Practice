Problem: https://codeforces.com/contest/2254/problem/B

Solution:
import java.util.*;

class Main {


    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int t = Integer.parseInt(sc.next());

        while(t-->0) {
            int n = Integer.parseInt(sc.next());
            String s = sc.next();
            
            int batch_count=1;
            for(int i=1;i<s.length();i++) {
                if(s.charAt(i)!=s.charAt(i-1)) batch_count++; 
            }

            int final_ans = batch_count;

            if(final_ans==1) {
                System.out.println(final_ans);
                continue;
            }

            for(int i=1;i<s.length()-1;i++) {
                int new_batch = batch_count;

                if(s.charAt(i)!=s.charAt(i-1) && s.charAt(i)!=s.charAt(i+1)) {
                    new_batch--;
                }

                if(s.charAt(i-1)==s.charAt(i+1)) {
                    new_batch--;
                }

                final_ans = Math.min(final_ans,new_batch);
            }

            System.out.println(final_ans);
        }
    }
    
}
