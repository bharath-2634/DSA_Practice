Question:https://leetcode.com/problems/subsets-ii/description/

Solution:
class Solution {
    public List<List<Integer>> subsetsWithDup(int[] nums) {
        Arrays.sort(nums);
        List<List<Integer>> res = new ArrayList<>();
        getSubsetsWithDup(nums,res,new ArrayList<>(),0);

        return res;
    }

    public void getSubsetsWithDup(int[] nums,List<List<Integer>> res,List<Integer> ds,int idx) {
        res.add(new ArrayList<>(ds));

        for(int i=idx;i<nums.length;i++) {
            if(i>idx && nums[i]==nums[i-1]) continue;
            ds.add(nums[i]);
            getSubsetsWithDup(nums,res,ds,i+1);
            ds.remove(ds.size()-1);
        }
    }
}
