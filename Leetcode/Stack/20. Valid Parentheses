Question: https://leetcode.com/problems/valid-parentheses/description/

Solution:
class Solution {
    public boolean isValid(String s) {
        Stack<Character> st = new Stack<>();
        for(char c : s.toCharArray()) {
            if(c=='(' || c=='{' || c=='[') {
                st.push(c);
            }else {
                if(st.isEmpty()) return false;
                char end = st.peek();
                if((end=='(' && c!=')') || (end=='{' && c!='}') || (end=='[' && c!=']')) {
                    return false;
                }
                st.pop();
            }
        }

        return st.isEmpty();
    }
}
