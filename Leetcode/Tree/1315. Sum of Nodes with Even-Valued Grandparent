Question : https://leetcode.com/problems/sum-of-nodes-with-even-valued-grandparent/

Solution  :
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    private int grandParent_sum = 0;

    public void getSum(TreeNode node,TreeNode parent,TreeNode grandParent) {

        if(node==null) return;

        if(grandParent!=null && grandParent.val%2==0) {
            grandParent_sum+=node.val;
        }

        getSum(node.left,node,parent);
        getSum(node.right,node,parent);

    }

    public int sumEvenGrandparent(TreeNode root) {
        
        if(root==null) return 0;
        
        getSum(root,null,null);

        return grandParent_sum;
    }
}
