Question:https://leetcode.com/problems/arithmetic-slices/

Solution:
/*
class Solution {

    public boolean checkAM(int[] nums,int start,int end) {
        
        int diff = -1;
        if((end-start+1)<3) return false;

        for(int i=start+1;i<=end;i++) {
            int current_diff = nums[i] - nums[i-1];
            if(diff==-1) diff = current_diff;
            // System.out.println(current_diff + " " + diff + " " + nums[i]);
            if(current_diff!=diff) return false;
        }

        return true;
    }

    public int numberOfArithmeticSlices(int[] nums) {
        int count=0;
        // Arrays.sort(nums);
        for(int i=0;i<nums.length;i++) {
            for(int j=i;j<nums.length;j++) {
                // if((j-i)<3) continue;
                if(checkAM(nums,i,j)) {
                    count++;
                }
            }
        }

        return count;
    }
}*/



class Solution {
    public int numberOfArithmeticSlices(int[] nums) {
        int count=0,maxCount=0;

        if(nums.length<3) return 0;

        int prev_diff = nums[1]- nums[0];

        for(int i=2;i<nums.length;i++) {
            int current_diff = nums[i] - nums[i-1];
            // System.out.println(current_diff + " " + prev_diff);
            if(current_diff==prev_diff) {
                count++;
                maxCount+=count;
            }else {
                count=0;
            }
            prev_diff = current_diff;
        }

        return maxCount;
    }
}
