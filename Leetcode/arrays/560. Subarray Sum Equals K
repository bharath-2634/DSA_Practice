Question : https://leetcode.com/problems/subarray-sum-equals-k/

Solution : 
class Solution {
    public int subarraySum(int[] nums, int k) {
        
        // int count=0;

        // for(int i=0;i<nums.length;i++) {
        //     int target = 0;
        //     for(int j=i;j<nums.length;j++) {
        //         target+=nums[i];
        //         if(target==k) count++;
        //     }
        // }
        // return count;

        Map<Integer,Integer> fMap = new HashMap<>();

        fMap.put(0,1);
        int sum=0,count=0;

        for(int num : nums) {
            sum+=num;

            if(fMap.containsKey(sum-k)) {
                count+=fMap.get(sum-k);
            }

            fMap.put(sum,fMap.getOrDefault(sum,0)+1);
        }

        return count;
    }
}
