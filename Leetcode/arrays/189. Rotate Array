Question : https://leetcode.com/problems/rotate-array/
Solution  :
class Solution {
    /* This is T.C O(n) and S.C O(n)
    public void rotate(int[] nums, int k) {

        int[] tempArr = new int[nums.length];
        k = k%nums.length;
        for(int i=0;i<nums.length;i++) {
            tempArr[(i+k)%nums.length] = nums[i];
        }

        for(int i=0;i<tempArr.length;i++) {
            nums[i] = tempArr[i];
        }
    }
    */

    public void rotate(int[] nums, int k) {
        k%=nums.length;
        rotateArray(0,nums.length-1,nums);
        rotateArray(0,k-1,nums);
        rotateArray(k,nums.length-1,nums);

    }

    public void rotateArray(int start,int end,int[] nums) {
        
        while(start<end) {
            int temp = nums[start];
            nums[start] = nums[end];
            nums[end] = temp;
            start++;
            end--;
        }
    }
}

/* 

    new_position = (old_position + k) % n

    7 6 5 4 3 2 1  (reverse The whole array)
    5 6 7 4 3 2 1  (reverse the first k elements)
    5 6 7 1 2 3 4  (reverse from k->n-1)
 */
